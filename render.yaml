services:
  - type: web
    name: cashify-dss
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn cashify_dss:server --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
